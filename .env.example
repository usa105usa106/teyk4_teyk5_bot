TELEGRAM_BOT_TOKEN=123456:replace_me
# Optional: restrict access to one Telegram user id, e.g. 123456789
OWNER_TELEGRAM_ID=
# Generate once with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=
# Use 1 only after paper testing. Live trading still requires Telegram toggle.
ALLOW_LIVE_TRADING=0
